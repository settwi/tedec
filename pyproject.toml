[build-system]
requires = ["setuptools >= 61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["tedec"]

[project]
version = "0.1"
name = "tedec"
dependencies = [
    "astropy",
    "matplotlib",
    "scipy",
]
